import './Error.css';

// This page will be used to display an error message when something goes wrong in the application. A typical example is Error 404 (page not found).
const Error = () => {
    return (
        <div>Error</div>
    );
}

export default Error;